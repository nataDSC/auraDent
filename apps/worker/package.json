{
  "name": "@auradent/worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "migrate:local": "tsx src/migrate.ts",
    "readback:local": "tsx src/readback.ts",
    "run:local": "tsx src/local-runner.ts",
    "test": "tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@auradent/ingestion": "file:../../packages/ingestion",
    "@auradent/shared": "file:../../packages/shared",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/pg": "^8.15.5",
    "typescript": "^5.8.3"
  }
}
